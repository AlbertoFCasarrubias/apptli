import { ScheduleHourDirective } from './schedule-hour.directive';

describe('ScheduleHourDirective', () => {
  it('should create an instance', () => {
    const directive = new ScheduleHourDirective();
    expect(directive).toBeTruthy();
  });
});
