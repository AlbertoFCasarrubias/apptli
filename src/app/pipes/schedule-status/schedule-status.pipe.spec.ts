import { ScheduleStatusPipe } from './schedule-status.pipe';

describe('ScheduleStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
